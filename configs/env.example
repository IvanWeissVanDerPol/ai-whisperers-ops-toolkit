# Hermes API Keys
DEEPSEEK_API_KEY=REPLACE_ME
TELEGRAM_BOT_TOKEN=REPLACE_ME
WHATSAPP_ENABLED=REPLACE_ME
GATEWAY_ALLOW_ALL_USERS=REPLACE_ME
WHATSAPP_API_KEY=REPLACE_ME

API_SERVER_KEY=REPLACE_ME

# LiteLLM proxy (Groq free tier)
LITELLM_API_KEY=REPLACE_ME
GITHUB_TOKEN=REPLACE_ME

# Cloudflare API token (for Cloudflare MCP server)
CLOUDFLARE_API_TOKEN=REPLACE_ME

# Exa Search API
EXA_API_KEY=REPLACE_ME

# Brave Search API
BRAVE_API_KEY=REPLACE_ME

# Stripe API (test mode)
STRIPE_SECRET_KEY=REPLACE_ME
STRIPE_PUBLISHABLE_KEY=REPLACE_ME
STRIPE_RESTRICTED_KEY=REPLACE_ME
OPENROUTER_API_KEY=REPLACE_ME
# Hindsight memory provider - local mode
HINDSIGHT_MODE=REPLACE_ME
HINDSIGHT_BANK_ID=REPLACE_ME
HINDSIGHT_BUDGET=REPLACE_ME
WHATSAPP_MODE=REPLACE_ME
WHATSAPP_ALLOWED_USERS=REPLACE_ME
WHATSAPP_HOME_CHANNEL=REPLACE_ME
WHATSAPP_HOME_CHANNEL_THREAD_ID=REPLACE_ME
OPENAI_API_KEY=REPLACE_ME
ZAI_API_KEY=REPLACE_ME
GOOGLE_CLIENT_ID=REPLACE_ME
GOOGLE_CLIENT_SECRET=REPLACE_ME
OPENCODE_ZEN_API_KEY=REPLACE_ME
OPENCODE_GO_API_KEY=REPLACE_ME

# Brave Search (correct env var for Hermes v0.14.0)
BRAVE_SEARCH_API_KEY=REPLACE_ME

# X (Twitter) Search — requires XAI_API_KEY or SuperGrok OAuth
# XAI_API_KEY=your-xai-key-here

# Microsoft Teams — requires app_id, app_password, tenant_id
# TEAMS_APP_ID=
# TEAMS_APP_PASSWORD=
# TEAMS_TENANT_ID=
XAI_API_KEY=REPLACE_ME
HERMES_DASHBOARD_TOKEN=REPLACE_ME
ELEVENLABS_API_KEY=REPLACE_ME

FAL_KEY=REPLACE_ME

CLOUDFLARE_ACCOUNT_ID=REPLACE_ME
R2_ACCESS_KEY_ID=REPLACE_ME
R2_SECRET_ACCESS_KEY=REPLACE_ME
R2_ENDPOINT=REPLACE_ME
R2_BUCKET=REPLACE_ME

R2_PUBLIC_BASE_URL=REPLACE_ME
WHATSAPP_REPLY_PREFIX=REPLACE_ME
WHATSAPP_MAX_MESSAGE_LENGTH=REPLACE_ME
WHATSAPP_CHUNK_DELAY_MS=REPLACE_ME
WHATSAPP_SEND_TIMEOUT_MS=REPLACE_ME

# Featherless AI (Premium - unlimited)

# Featherless AI (Premium - unlimited)

# Featherless AI (Premium - unlimited)
FEATHERLESS_API_KEY=REPLACE_ME

NOUS_PORTAL_KEY=REPLACE_ME
VERCEL_API_TOKEN=REPLACE_ME
HERMES_DASHBOARD_SESSION_TOKEN=REPLACE_ME
HUGGINGFACE_API_KEY=REPLACE_ME
TOGETHER_API_KEY=REPLACE_ME
CEREBRAS_API_KEY=REPLACE_ME
ANTHROPIC_API_KEY=REPLACE_ME
MINIMAX_API_KEY=REPLACE_ME
MINIMAX_CN_API_KEY=REPLACE_ME
MCP_COMPOSIO_API_KEY=REPLACE_ME

# === Meta stack (Facebook + Instagram + Ads) — Ai-Whisperers 2026-07 ===
# Composio: dashboard.composio.dev → Connect → API Key
# Pipeboard: pipeboard.co → API Token (free plan)
# Postiz: postiz.com → Settings → API Key
# META_* only needed if you enable graph-api-cli fallback MCP
PIPEBOARD_API_TOKEN=REPLACE_ME
POSTIZ_API_KEY=REPLACE_ME
META_PAGE_TOKEN=REPLACE_ME
META_APP_ID=REPLACE_ME
META_APP_SECRET=REPLACE_ME
META_PAGE_ID=REPLACE_ME
META_IG_USER_ID=REPLACE_ME

# Trusted proxy origins for dashboard web_server (CloudFlare/Tailscale funnel).
# Comma-separated hostnames. The peer TCP is still loopback — the proxy
# terminates TLS and forwards to 127.0.0.1.
HERMES_TRUSTED_PROXY_ORIGINS=REPLACE_ME
