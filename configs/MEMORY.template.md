# MEMORY template — copy to ~/.hermes/memories/MEMORY.md and customize

**Skill curation constraint (2026-07-29)**: Skills I create this session via `skill_manage(action='create')` / `write_file` are user-owned; background curator refuses to patch them autonomously (must recommend `hermes curator adopt <name>` instead). Workaround: list skill updates needed in the final summary as "Skills to adopt for next session". Skills with metadata.hermes block (simplify-code, pr-review) are curator-managed + off-limits; bundled ones are off-limits regardless.
§
**Thesis idea-atlas + repo-split + global-baseline workflow (2026-07-31)**: When Ivan says "lets do 1000 / not married / run the wizard" → cartesian-then-handcrafted-then-interactive (1,439 ideas, 6-dim scoring, wizard CLI with 7 preset profiles, strict-filter-0 = surface trade-off not auto-relax). When "analyze and document for me to read" → 3 layered formats (cheat sheet 5min / deep analysis 30min / per-idea docs × 60-100). When "pass me the github links" → direct blob/tree URLs in tables (entry points, per-idea, recent commits). When "move X to new repo and remove from Y" → save final enrichment snapshot FIRST, then `gh repo create` (defaults to `main` not `master`), `shutil.copytree` skipping raw_html_snapshots + legacy ckpts, bootstrap README + .gitignore, `git rm -r -f` source, `rm -rf` untracked. When "research more universities / LATAM / Asia / 1st world / China" → lead with arXiv search + HuggingFace (Tsinghua/IIIT/CNTI/NTU/KTH all blocked/private from this env), 5+ varied arXiv queries per session, score on `p_relevance` (P1/P2/P3/Other) + `p_relevance_score` (0-100) + `differentiation_for_ivan`, target 50-60 findings. HF insight: 38 Guaraní datasets vs 0 Aymara/Quechua/Nahuatl/Mapuche — P3 is the only proposal with pre-built indigenous-language datasets. Full patterns: skill `academic-thesis-corpus-mining` Phases 4-8.
§
**MRI context (Ivan, accession 519328, Centro Medico Bautista, Asuncion, 2026-07-22)**: L4-L5 Modic 1 + L4 hemangioma (incidental) + right SI joint BME + diffuse right hemipelvis T2 hyperintensity (48% axial slices) + bilateral hydroceles → working hypothesis axial spondyloarthritis + L4-L5 discogenic pain. Pending formal radiology report + HLA-B27/CRP/ESR + rheumatology referral. Reports at REPLACE_ME.html (6.5MB) + 05_FINAL_DETAILED_REPORT.md (57KB), commit 3348d8d1. Surface on any back/SI/buttock mention.
§
**Annotated imagery demand (2026-07-31)**: For any visual finding, ship BOTH original source + red-circle/ellipse annotated copy. Verify placement with vision_analyze BEFORE declaring done — initial hard-coded coordinates have been wrong (disc level off by 2 vertebra, circles in empty space).
§
**Plain-language explanation mode (2026-07-31)**: After technical report, "explain the results" → PLAIN-LANGUAGE version with analogies, confidence levels, "what to do this week" / questions-to-ask-doctor sections. Never assume technical literacy for medical results.
§
**Vision tool failure pattern (2026-07-31)**: vision_analyze intermittently returns "[screenshot removed to save context]" or 404 on certain image URLs. When it fails: (1) try local path instead of URL, (2) try with explicit question, (3) if 5-failure hard-stop triggers, switch strategy: load image locally and reason from existing context, or break the image into smaller pieces.
§
**R10 self-healing foundation (2026-07-31)**: Fixed 11 broken crons that R9 observability revealed. Pattern: when crons look "broken", check (1) absolute vs relative script path, (2) dead model refs (claude-3.5-sonnet removed 2026-07), (3) config keys (TELEGRAM_HOME_CHANNEL, chat IDs). 67→70 crons.
§
**CRITICAL GOTCHA — hermes cron argv (2026-07-31)**: `hermes cron create --script "script.py --args"` registers the LITERAL string as filename. Runner searches for `script.py --args`, returns `Script not found`. CRONS WITH ARGS MUST use wrapper .sh: `wrapper.sh` contains `#!/usr/bin/env bash\nexec python3 /root/.hermes/scripts/script.py --args`, then `--script wrapper.sh`. 7 crons fixed in R10 via this pattern. Apply to all future arg-based crons.
§
**R23 user-feedback rules (2026-08-03)**: (1) Always start with `/api/health`, never rediscover. (2) Decompose broad asks into R-N tasks. (3) Verify with curl/exit codes, never narrative. (4) wrapper.sh for cron args. (5) Trust infrastructure after R17+. Full guide: /REPLACE_ME.md (18.3KB). Updated skills: agent-persona-design (R23 Lessons), one-three-one-rule (verification-first), avoid-ai-writing (Hermes voice patterns).
