STEP 1: Run `python3 ~/.hermes/skills/hermes-dojo/scripts/monitor.py --days 1`. Capture top 3 weaknesses (lowest success rate OR most user corrections OR new skill gap).
STEP 2: For each weakness, pick the right fix:
  - Existing skill failing -> patch with skill_manage action='patch' to add error handling or edge case coverage
  - No skill exists for a recurring need -> create with skill_manage action='create'
  - Pattern is one-off -> log to ~/.hermes/logs/dojo-provenance.log but don't change anything
STEP 3: ONLY change ONE thing per day. Reversible. Log timestamp + action + target to provenance log.
STEP 4: Re-run monitor.py. If success rate improved, great. If not, roll back.
STEP 5: Deliver to local. Format: 3-line summary with what changed and why.